import test from "ava";
import { Primitives, Nodes } from "./actions";

test("[Actions] [Primitive] arbitrarySet()", (t) => {
    // TODO Should return value of the same type
    // TODO Should return the same value
});

test("[Actions] [Primitive] arbitraryTransform()", (t) => {
    // TODO Should return value of the same type
    // TODO Should apply transform properly
    // TODO Should reject non-function transforms
});

test("[Actions] [Basic actions] innerHTML()", (t) => {
    // TODO
});

test("[Actions] [Basic actions] opacity()", (t) => {
    // TODO
});

test("[Actions] [Basic actions] transform()", (t) => {
    // TODO
});

test("[Actions] [Basic actions] appendChild()", (t) => {
    // TODO
});

test("[Actions] [Basic actions] appendSibling()", (t) => {
    // TODO
});

test("[Actions] [Basic actions] replace()", (t) => {
    // TODO
});

test("[Actions] [Basic actions] remove()", (t) => {
    // TODO
});
